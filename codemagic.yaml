workflows:
  android-build:
    name: Android Release Build
    max_build_duration: 30
    instance_type: mac_mini_m1
    environment:
      groups:
        - android-signing
      vars:
        PACKAGE_NAME: "br.com.yson.controle.de.obras"
      node: latest
    triggering:
      events:
        - push
      branch_patterns:
        - pattern: 'main'
          include: true
    scripts:
      - name: Configurar Chave de Assinatura
        script: |
          # Transforma o texto Base64 de volta no arquivo de chave real na pasta do build
          echo $CM_KEYSTORE | base64 --decode > $CM_BUILD_DIR/controledeobra.p12
      - name: Build Android App Bundle
        script: |
          # Da permissao de execucao ao arquivo wrapper existente no projeto
          chmod +x gradlew
          
          # Compila o app de forma limpa usando a versao de Gradle nativa do seu projeto
          ./gradlew clean bundleRelease \
            -Pandroid.injected.signing.store.file=$CM_BUILD_DIR/controledeobra.p12 \
            -Pandroid.injected.signing.store.password=$CM_KEYSTORE_PASSWORD \
            -Pandroid.injected.signing.key.alias=$CM_KEY_ALIAS \
            -Pandroid.injected.signing.key.password=$CM_KEY_PASSWORD
    artifacts:
      - app/build/outputs/bundle/release/*.aab
